

在包级别声明的变量会在main入口函数执行前完成初始化

局部变量将在声明语句被执行到的时候完成初始化。

```go
var i, j, k int                 // int, int, int
var b, f, s = true, 2.3, "four" // bool, float64, string

```

简短变量声明
+ 只能在函数内部
+ 也支持声明和初始化一组变量
```go
i, j := 0, 1
```

```go
i, j := 1, 2

x, j := 3, 5 // 这里的 j 会被覆盖

```


对于每一个变量必然有对应的内存地址。通过指针，我们可以直接读或更新对应变量的值，而不需要知道该变量的名字（如果变量有名字的话）。